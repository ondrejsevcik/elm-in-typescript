{
  "name": "elm-typescript",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "check": "tsc --noEmit",
    "runTests": "mocha --reporter spec --compilers ts:ts-node/register tests/**/*.test.ts",
    "test": "yarn run check && yarn run runTests"
  },
  "devDependencies": {
    "@types/chai": "^4.1.6",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.3"
  }
}
